{
  "name": "<%= _.slugify(appname) %>",
  "version": "0.0.0",
  "devDependencies": {
    <% if (cssPreProcessor === 'Scss') { %>"node-sass": "*",<% } %>
    <% if (cssPreProcessor === 'Less') { %>"less": "*",<% } %>
    "autoprefixer": "*",
    "grunt": "*",
    "grunt-contrib-clean": "*",
    "grunt-contrib-jshint": "*",
    "grunt-livestyle": "*",
    "grunt-newer": "*",
    "grunt-reduce": "*",
    "jit-grunt": "*",
    "jshint-stylish": "*",
    "time-grunt": "*"
  }
}
